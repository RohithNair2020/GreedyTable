import React from "react";
import styles from "./settingsComponent.module.css";

const SettingsComponent = () => {
  return (
    <div className={styles.settingsComponentContainer}>SettingsComponent</div>
  );
};

export default SettingsComponent;
